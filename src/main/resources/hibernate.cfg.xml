<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/TestingSystem</property>
        <property name="connection.username">root</property>
        <property name="connection.password">12345678</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <property name="hibernate.format_sql">true</property>

        <property name="hbm2ddl.auto">update</property>
        <mapping class="org.example.entity.account.Jobs"/>
        <mapping class="org.example.entity.account.JobDetails"/>
        <mapping class="org.example.entity.account.Employees"/>

        <mapping class="org.example.entity.product.Category"/>
        <mapping class="org.example.entity.product.Products"/>
        <mapping class="org.example.entity.Department"/>

    </session-factory>
</hibernate-configuration>